# JWT

Small library for
* Generating json web tokens with a predefined set of claims using asymmetric assymetric encryption EdDSA (Ed25519).
* Validating and parsing json web tokens.

Private and public keys are stored as environment variables.
